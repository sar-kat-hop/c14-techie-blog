<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="/css/jass.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous"></head>

<body>
    <div class="jumbotron">
        <div class="container">
            <h1 class="display-5"> The Tech Blog </h1>
            <p class="lead"></p>

            <hr class="my-2"

            {{#if logged_in}}
                <div class="container">
                    <nav>
                        <button class="btn btn-primary" id="logout-btn">Log out</button>
                    </nav>
                </div>
            {{/if}}

            <div class="container">
                {{{ body }}}
            </div>

        </div>
    </div>

    <footer>
    </footer>

    <!-- show logout script only if user is still logged in -->
    {{#if logged_in}}
    <script src="../../public/js/logout.js"></script>
    {{/if}}
</body>

{{!-- <script src="../../public/js/login.js"></script> {{! testing whether adding this resolves logging in issue}} --}}
{{!-- <script src="../public/js/logout.js"></script> --}}

</html>
